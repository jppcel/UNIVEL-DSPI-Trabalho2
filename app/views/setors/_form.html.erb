<%= form_for(setor) do |f| %>
  <% if setor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(setor.errors.count, "error") %> prohibited this setor from being saved:</h2>

      <ul>
      <% setor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nomeSetor, 'Nome do Setor' %>
    <%= f.text_field :nomeSetor %>
  </div>

  <div class="field">
    <%= f.label :letraSetor, 'Letra' %>
    <%= f.text_field :letraSetor %>
  </div>

  <div class="actions">
    <%= f.submit class: 'button'  %>
  </div>
<% end %>
